---
title: Game Logic
description: How the server processes input, physics, collisions, and scoring.
---

`GameManager` (`flaggi.server.common`) runs one game instance. It processes player input, simulates physics, handles collisions, manages flag mechanics, and sends state updates.

## Game tick

Each tick of `GameManager.update()`:

1. **Process input** - read latest `ClientStateUpdate` per player
2. **Move players** - apply movement with collision detection
3. **Update animations** - set sprite state based on movement direction
4. **Spawn bullets** - create bullet if player is shooting and cooldown expired
5. **Tick bullets** - advance positions, check tree and player collisions
6. **Tick flags** - check pickup, carry, capture, and return logic
7. **Send state** - build per-player `ServerStateUpdate` and send via UDP

## Movement

Player speed is `3.0 pixels per tick`. Movement uses a sliding collision system:

1. **Try diagonal** - move in both X and Y
2. **If blocked, try horizontal only** - move in X
3. **If blocked, try vertical only** - move in Y
4. **Clamp** - keep player within room boundaries (0 to roomWidth/Height)

```
tryMove(player, dx, dy, obstacles):
  newX = player.x + dx
  newY = player.y + dy
  for each obstacle:
    if AABB overlaps(player at newPos, obstacle):
      return false
  player.setX(newX)
  player.setY(newY)
  return true
```

## Collision detection

All collisions use **AABB** (Axis-Aligned Bounding Box) checks. Each game object has a hitbox defined by offset + dimensions relative to its origin:

```
worldLeft   = x + collisionOffsetX
worldTop    = y + collisionOffsetY
worldRight  = worldLeft + collisionWidth
worldBottom = worldTop + collisionHeight
```

Two objects overlap when their world-space bounding boxes intersect on both axes.

## Combat

### Shooting

- Players press `KEY_SHOOT` to fire
- Cooldown: `400ms` between shots (`SHOOT_COOLDOWN_MS`)
- Bullets spawn at the player's position, aimed toward the mouse cursor
- Direction is normalized and scaled by `BULLET_SPEED = 8.0 px/tick`

### Bullets

Each tick:

1. Advance bullet position by its velocity vector
2. Check collision with trees → destroy bullet
3. Check collision with players (not the bullet's owner) → apply damage, destroy bullet
4. Check lifetime (max 120 ticks = 2 seconds) and room bounds → destroy if expired

### Damage

- Bullet damage: `25.0`
- Player max HP: `100.0`
- **4 hits to kill**
- On death: instant respawn at spawn point, full HP restore, drop any carried flag

## Flag mechanics

Each game has two flags: RED and BLUE. The flag state machine:

```
AT_BASE ──pickup──→ CARRIED ──capture──→ AT_BASE (score!)
   ▲                    │
   │                    │ (carrier killed)
   │                    ▼
   └──────return───── DROPPED
```

### Rules

- **Pickup**: A player touching the _enemy_ flag picks it up (AT_BASE or DROPPED → CARRIED)
- **Carry**: The flag follows the carrier's position
- **Capture**: The carrier touches their _own_ flag's base → score + both flags return to base
- **Drop**: If the carrier dies, the flag drops at their position (CARRIED → DROPPED)
- **Return**: A player touching their _own_ dropped flag returns it to base (DROPPED → AT_BASE)

### Win condition

First team to **3 captures** wins. The server sends `ServerEndGame` to both clients with the winner's name.

## State updates

After all logic runs, `GameData.getServerUpdateData()` builds personalized `ServerStateUpdate` messages for each player:

- `me` - the player's own `ServerGameObject`
- `other` - all other objects (opponent, trees, bullets, flags)
- `tick` - current tick number

Carried flags are excluded from the `other` list since their position is implicit (same as carrier).

## Key constants

| Constant             | Value       | Description          |
| -------------------- | ----------- | -------------------- |
| `PLAYER_SPEED`       | 3.0 px/tick | Movement speed       |
| `BULLET_SPEED`       | 8.0 px/tick | Bullet speed         |
| `BULLET_DAMAGE`      | 25.0        | Damage per hit       |
| `MAX_HP`             | 100.0       | Player health        |
| `SHOOT_COOLDOWN_MS`  | 400         | Shot cooldown        |
| `MAX_LIFETIME_TICKS` | 120         | Bullet lifetime (2s) |
| `CAPTURES_TO_WIN`    | 3           | Win condition        |
